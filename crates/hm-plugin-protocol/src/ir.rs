//! Pipeline IR types re-exported from `hm_pipeline_ir`, so the wire types
//! in this crate and their consumers share a single definition.

pub use hm_pipeline_ir::{Cache, DurationMs, Step, StepAction};
